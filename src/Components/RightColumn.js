import React from 'react';

const RightColumn= () => (
    <div>
        <p>this is where the small bio, social media icons, and archive will go.</p>
    </div>
);

export default RightColumn;